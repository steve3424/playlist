- auth refactor
  - put in middlewares?
  - should be just string values instead of int/hierarchy
  - tests
  
- discuss how song updates will be shared live
    - can full list be shared?
      what if new user joins, shares empty list, and
      this deletes other users lists
    - should we only share updates by default?
    - multiple modes: update/full
    - up next vs add to end of list
- handle different message types in ws server
- finish out db tables
- integrate w/ google drive for storage
- remove uvicorn.error to avoid stack traces on custom exceptions